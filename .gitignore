node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
.vite
coverage
.lighthouseci
*.local

# Private specification — kept locally, never committed
cahier-des-charges.html

# Supabase CLI local artefacts
supabase/.temp
supabase/.branches

# Local secrets — Google OAuth credentials, never committed
supabase/.env
client_secret_*.json
