node_modules
dist
dist-ssr
coverage
package-lock.json
cahier-des-charges.html

# Vendored Stockfish build — never reformat
public/stockfish

# Generated by scripts/gen-db-types.sh — must match the CLI output byte for byte
src/lib/database.types.ts

# Written by release-please; its markdown does not match Prettier's and it owns
# the file, so reformatting it would fight the next release.
CHANGELOG.md

# Supabase CLI local artefacts
supabase/.temp
supabase/.branches
